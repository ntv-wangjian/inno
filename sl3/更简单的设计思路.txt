以前在Loader中去维护和管理每个命令，更多的复杂性消耗在队列的管理和逐个维护状态上，太绕了，实在是设计的失误。

新的思路：
service loader 进程只是简单的负责扫描任务文件，并启动任务，发射后不管。
	对于once命令，简单启动即可；
	对于service命令，由每个process进程单独维护自己的子进程。

process进程，负责启动命令。
	监督命令的运行，一旦停止就再次拉起。
	监督任务文件的存在，一旦不再就立即终止命令。



日志啊，进程启动与判断啊，继续使用积累的经验。

详细实现见sl3